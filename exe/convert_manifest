#!/usr/bin/env ruby
# frozen_string_literal: true

require 'misc/convert_manifest'

old_manifest = ARGV[0]
depth = ARGV[1] || '1'
new_manifest = ConvertManifest.convert_manifest(filename: old_manifest, depth: depth.to_i)
File.open("#{old_manifest}.new", 'w') { |file| file.write(new_manifest) }
